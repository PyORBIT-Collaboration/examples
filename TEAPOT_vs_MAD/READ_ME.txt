Two directories 
MAD - with mad8.3 calculation results 
pyTEAPOT - with pyORBIT calculations with TEAPOT lattice and TEAPOT_MATRIX_Lattice.

The both directories should come together. The pyTEAPOT directory uses the MAD directory.
